/* This exercise converts white space characters into their ascii escape sequences (e.g. tab becomes \t).
 * A switch statement should be used */

/* TODO escape(s, t)
 * while(s(i) !=n '\0'
 *     switch (inChar)
 *     case \n
 *         append '\\' to string t
 *         append 't' to string t
 *     case \t
 *         append '\\' to string t
 *         append 'n' to string t
 *     continue for all white space chars
 *     default
 *         append s(i) to string t
 *     append '\0' */

 /* TODO simple driver code
  * write a phrase that intersperse white space chars with normal chars
  * send the string, along with another empty string to escape(s, t)
  * print out first s and then t */
